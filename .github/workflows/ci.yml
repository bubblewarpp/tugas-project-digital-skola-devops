name: Secure CI Pipeline - Notes App (DevSecOps)

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  unit_test:
    name: Unit Test (pytest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r dev-requirements.txt

      - name: Run pytest
        env:
          PYTHONPATH: .
        run: pytest -q


  sast_bandit:
    name: SAST (Bandit)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Run Bandit
        run: |
          pip install bandit
          bandit -r . -ll


  sca_pip_audit:
    name: SCA (pip-audit)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Run pip-audit
        run: |
          pip install pip-audit
          pip-audit


  secret_scan_gitleaks:
    name: Secret Scan (Gitleaks)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run Gitleaks
        uses: gitleaks/gitleaks-action@v2
        with:
          fail: true


  build_and_push:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    needs:
      - unit_test
      - sast_bandit
      - sca_pip_audit
      - secret_scan_gitleaks

    steps:
      - uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build & Push Image
        uses: docker/build-push-action@v5
        with:
          context: .
          pull: true
          no-cache: true
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/notes-app:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/notes-app:${{ github.sha }}
  image_scan_trivy:
    name: Image Scan (Trivy)
    runs-on: ubuntu-latest
    needs: build_and_push
    steps:
      - name: Inspect Python package versions in image
        run: |
          docker pull ${{ secrets.DOCKERHUB_USERNAME }}/notes-app:${{ github.sha }}
          docker run --rm ${{ secrets.DOCKERHUB_USERNAME }}/notes-app:${{ github.sha }} sh -c "python -m pip show wheel jaraco.context || true"
          docker run --rm ${{ secrets.DOCKERHUB_USERNAME }}/notes-app:${{ github.sha }} sh -c "find /usr -type d \( -name 'wheel-*.dist-info' -o -name 'jaraco.context-*.dist-info' \) | sort || true"

      - name: Trivy Scan Image
        uses: aquasecurity/trivy-action@0.33.1
        env:
          TRIVY_DISABLE_VEX_NOTICE: "true"
        with:
          image-ref: ${{ secrets.DOCKERHUB_USERNAME }}/notes-app:${{ github.sha }}
          severity: CRITICAL,HIGH
          ignore-unfixed: true
          exit-code: 1

